#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "sqr.h"
int main()
{
    int a, b, c;
    printf("Enter a,b,c\n");
    scanf("%d %d %d", &a, &b, &c);
    answer(a, b, c);
    return 0;
}
